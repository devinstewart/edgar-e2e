{
  "name": "edgar-e2e",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "lab -a @hapi/code -t 100 -L -m 10000",
    "build": "(cd ./client && npm install && ng build --prod) && npm install --prod"
  },
  "author": "Devin Stewart",
  "license": "MIT",
  "dependencies": {
    "@hapi/hapi": "^20.1.0",
    "@hapi/inert": "^6.0.3",
    "@hapi/wreck": "^17.1.0",
    "joi": "^17.4.0"
  },
  "devDependencies": {
    "@hapi/code": "^8.0.2",
    "@hapi/lab": "^24.1.0"
  }
}
